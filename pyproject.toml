[project]
name = "kafka-crypto-tracker-project"
version = "0.1.0"
description = "Kafka-based Cryptocurrency Market Event Tracker"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "confluent-kafka",
    "pydantic-settings>=2.14.1",
    "websockets",
]
